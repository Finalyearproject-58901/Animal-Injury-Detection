# Animal-Injury-Detection

🔥 Every Time You Open Terminal

Before running your project:
cd ~/Downloads/Animal-Injury-Detection-main
source venv/bin/activate

animal_injury_ai/
│
├── data/
├── models/
├── edge/
├── cloud/
├── api/
├── utils/
├── app/
└── main.py {
    🐾 main.py – Animal Injury Detection System (RTSP Optimized Version)

        This version:
        Uses RTSP stream
        Reduces delay (buffer size = 1)
        Skips frames
        Uses YOLO for animal detection
        Runs injury detection logic
        Displays live stream
}
